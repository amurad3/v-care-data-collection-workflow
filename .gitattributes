# Normalize line endings: store text files with LF in the repo,
# check out with the platform's native endings. Prevents CRLF/LF churn.
* text=auto

# Keep these explicitly LF
*.md   text eol=lf
*.csv  text eol=lf
*.json text eol=lf
